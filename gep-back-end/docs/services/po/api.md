# PO Service — API Reference

Rendered from the live OpenAPI spec at <http://localhost:3003/api/v1/openapi.json>, fetched into `docs/api/po.json` at build time. Re-run `./scripts/fetch-openapi.ps1` after changing routes.

!!swagger api/po.json!!
