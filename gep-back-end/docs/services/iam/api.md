# IAM — API Reference

Rendered from the live OpenAPI spec at <http://localhost:3001/api/v1/openapi.json>, fetched into `docs/api/iam.json` at build time. Re-run `./scripts/fetch-openapi.ps1` after changing routes.

!!swagger api/iam.json!!
