# Supplier Service — API Reference

Rendered from the live OpenAPI spec at <http://localhost:3002/api/v1/openapi.json>, fetched into `docs/api/supplier.json` at build time. FastAPI auto-generates this spec from the route signatures and Pydantic models, so it never drifts from the code.

!!swagger api/supplier.json!!
